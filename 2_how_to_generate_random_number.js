// 1. Generate a single random number
//const number = Math.random() * 10;

// console.log(Math.floor(number));

// 2. Generate a 100 random numbers
for (let index = 0; index < 100; index++) {
  const number = Math.random() * 100;
  console.log(Math.floor(number));
}
